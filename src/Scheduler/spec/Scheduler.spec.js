import Scheduler from '../Scheduler';

describe('Scheduler', () => {
  describe('Context Interface', () => {
    it('Should store scheduler context in an object with key of the family', () => {
      expect(true).toBeFalsy();
    });

    it('Should have the key of the current animation in the context', () => {
      expect(true).toBeFalsy();
    });

    it('Should have a reference to the timeline in the context', () => {
      expect(true).toBeFalsy();
    });

    it('Should have an done method in context that cancels current key animation', () => {
      expect(true).toBeFalsy();
    });

    it('Should have a goToKey method in context that cancels current key animation and goes to a key', () => {
      expect(true).toBeFalsy();
    });
  });
});


